{
  "name": "saas-storefront",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/worker",
    "apps/web"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:worker": "npm run dev --workspace=apps/worker",
    "dev:web": "npm run dev --workspace=apps/web",
    "build:api": "npm run build --workspace=apps/api"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
